spring:
  application:
    name: config-server
  cloud:
    config:
      server:
       git:
         uri: https://github.com/xdongs2009/spring-clound-demo/config/
         search-paths: respo
         username:
         password:
      label: master
server:
  port:8086


